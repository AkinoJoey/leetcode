# NOTES

## 学習者プロフィール（2026-07-19 初回セッションで確認）
- ゴール：技術面接の突破（短期集中、1〜3ヶ月）
- 現在地：Easy は解ける／Medium で詰まる
- 言語：Python
- 出力言語：日本語（`CLAUDE.md` の指定）

## 教え方の方針
- ミッションが「面接突破」なので、常に**口頭で説明できる形**まで持っていく。
  計算量とトレードオフを言語化させることを毎レッスンに埋め込む。
- Medium で詰まる人の典型は「素朴解は出るが最適化に進めない」。
  レッスンは「パターン名 → 見分け方の合図 → テンプレート」の順で固定する。
- NeetCode Roadmap の依存順を進行の骨格にする。ただし1レッスン＝1トピックの一部に絞る。

## 確認済み（2026-07-19）
- 面接は **2026年10月以降**。準備期間 = 約10週間（〜09-30）
- 使える時間 = 平日1h／週末2〜3h → **週約10時間、総計約100時間**
- 模擬面接の経験なし。抵抗の有無は未確認だが、未経験であることは確定。

## 未確認・次回聞くこと
- 志望企業（分かれば頻出問題の重み付けができる）
- 模擬面接そのものへの心理的な抵抗の有無（未経験なだけか、避けたいのか）

## 計画上の判断
- 100時間では NeetCode 150 の完走は非現実的。**Blind 75 相当のコアを確実に**取り、
  余力でパターンごとの2問目・3問目に広げる方針とする。
- 最大リスクは技術力ではなく**口頭説明の未経験**。9月に入る前（8月下旬・50問時点）に
  初回の模擬面接を必ず入れる。ここは後回しにさせない。
- 週の設計：平日=新規パターン学習（1h＝レッスン+1問）、週末=復習と多問演習（interleaving）。

## 進行ログ
- Lesson 01: ハッシュによる O(n²) → O(n) 変換。**Group Anagrams を独力で完答**（LR-0001）。
  詰まりどころはアルゴリズムではなく Python のハッシュ可能性だった。
- Lesson 02: sliding window（可変長・最長型）。冒頭に LR-0001 で未確認だった
  「キー設計を計算量で比較する」観点を retrieval quiz として仕込んだ（spacing）。
  演習：Best Time to Buy and Sell Stock → Longest Substring Without Repeating → Character Replacement。
  → 結果：**Maximum Average Subarray I を自力で解けなかった**（解答を見て理解）。可変長窓の2問は未着手。
  LR-0004 の「定着していなければレッスンを1本これに割く」条件に該当したため、Lesson 03 を境界に全振りした。
- Lesson 03: 境界の検算に一本化。新パターンなし。代入検算（小さい具体値を手で数える）を手続きとして与え、
  ドリル6問＋ Maximum Average Subarray I の穴埋め3箇所（すべて境界）で確認。
  LR-0002 の教訓に従い**選択式を一切使わず記述式のみ**にした（`assets/input-check.js` を新規作成）。
  `reference/boundary-checklist.html` を新設。課題：643 を白紙から再実装 → 3（可変長窓）。
  → **643 を自力で完答**（LR-0005）。可変長窓（3）は未着手、次セッションの入口。

## 運用ルール（2026-07-20 合意）
- 演習コードの冒頭に **②素朴解 / ⑥計算量 / ⚠言えなかったこと** をコメントで記載して提出する。
  書式は `reference/interview-protocol.html` の「練習時の記録フォーマット」節。
- 順序厳守：**口に出す → 言えたことだけ書く**。補完して書かせない（推敲が入ると②が検出器として死ぬ）。
- `⚠ 言えなかったこと` を毎回読むこと。**ここが次のレッスンの題材になる。**
- ②のコメントは模擬面接（8月下旬）以降に外す。⑥は継続。
- **`⚠ 言えなかったこと` → `⚠ いちばん自信のない行` に変更（2026-07-20、LR-0006 を受けて）。**
  「なし」禁止。自覚できない誤りは自己申告欄に書けないため、必ず1つ出させる形式にする。

## 進行ログ（続き）
- Lesson 04: 償却論法。3（Longest Substring）はテンプレート参照で完答、境界エラーはゼロ。
  ただし⑥の根拠が「各要素は最大**1回**」と誤っており、かつ `⚠ なし` と自己申告（LR-0006）。
  → 「入るとき1回・出るとき1回＝2回」を `s="abba"` のトレースで数えさせ、**3文テンプレート**を与えた。
  この型は two pointers・単調スタックで再利用するので、以後の計算量説明はこの3文に統一する。
  空間の粒度（`O(min(n, 文字種))`）も併せて指導。
  課題：424 Character Replacement を**まず10分自力で**（テンプレートを開いたら正直に申告させる）。
- Lesson 05: `is_invalid` の導出。424 は**テンプレートを見ても解けなかった**（LR-0007）。
  原因は本人ではなく**資料側**：可変長テンプレートが `while is_invalid(state):` と抽象化し、
  424 の唯一の難所を関数名で隠していた。3 では valid が問題文に直書きだったため露呈しなかった。
  → 「**日本語 → 量 → 不等号**」の3段翻訳を明示。テンプレートに導出節と問題別の対応表を追記。
  課題：424 を白紙から（`# valid とは:` を最初の行に必須）→ 1004 Max Consecutive Ones III で同じ導出を反復。

- Lesson 06: ②⑥の精度。424 は白紙から自力完答（`is_invalid` の導出は1回で定着）。
  一方 **2セッション連続で⑥に誤り**（LR-0008）：空間 `O(min(1,26))`＝形だけの流用、
  `max(counter.values())` の O(26) 走査に言及なし（前回名指しで警告した箇所）、
  ②の素朴解に「最頻文字」が登場しない＝**詰まったときの誤った模型が②に残存**。
  → ⑥を自由記述から **rubric 付き**に変更（`reference/interview-protocol.html` に採点表）。
  空間は idiom 暗記をやめ「状態 → 最大要素数 → 入力依存か」の3段導出に。
  ②の検算法：**最適解の主役が素朴解に登場しているか**を確認する。
  課題：424 の⑥を rubric で書き直す → 1004。
- **`⚠ いちばん自信のない行` は初回から機能した**（挙げたのは `while` の条件式＝核心）。形式変更は成功、維持。
- rubric 化は成功（LR-0009）。3往復で②⑥が全項目クリア。空間は枠を与えただけで一発正解。
  時間の枠は [時間2]（毎回走るコスト）/[時間3]（償却で数える内側ループ）に**分割済み**。以後この形で運用。
  残る弱点は**ボトルネックの言語化**。「重なった部分を数え直している」は自力で出ず、今回は与えた。
  → 次に window 型を解くとき、この一文を言えるか再確認する（storage strength の検査）。
- 1004 を**10分で自力完答**。sliding window は4問通過、**獲得済みとみなして two pointers に移行**。
  `⚠` に `while 0 in counter and counter[0] > k` を挙げたのが的確（`defaultdict` のキー生成防御＝唯一議論の余地がある行）。
- Lesson 07: Two Pointers。難所は骨格ではなく **③「捨ててよい理由」**（＝この型における `is_invalid`）。
  LR-0007 の教訓により、テンプレートで抽象化せず**最初から開示**し、問題別の対応表も作った
  （`reference/two-pointers-template.html`）。計算量は Lesson 04 の3文がそのまま再利用できる点を強調。
  課題：167（助走・骨格がそのまま動く）→ 11 Container With Most Water（③が本番）。
- **rubric 更新（2回目）**：ボトルネック欄を「**何の再計算を避けたか**」に文言変更（LR-0010）。
  two pointers 用に「③ 捨ててよい理由」欄を追加。
- 状態の選択（1004 で int で足りるのに `defaultdict`）は未獲得。減点せず一行だけ触れた。次回以降も軽く反復。
- **167 を自力完答**（境界・1-indexed とも正しい）。`⚠` は `left < right`＝3回連続で急所を的中。
  一方 ⑥ で **`[時間1] logn回` → O(logn)** と誤答（LR-0011）。資料に O(n) と明記済みなのに上書きされた
  ＝**Lesson 07 が「捨てる」を核心語に据えたことで二分探索の模型を引き寄せた**（資料起因）。
  ③も2段目止まり（3段目＝捨てる側に良い答えが残っていない理由が欠落）。**対話では言えたのに書くと落ちる**。
- Lesson 08: ③と⑥を**一体で**扱う。③＝なぜ捨てるか（正しさ）／⑥＝1回でどれだけ捨てるか（速さ）。
  「log n が出てよいのは捨てる量が**残りの一定割合**のときだけ」を対比表で明示。
  two pointers の計算量説明を**ギャップ論法**（`right-left` が毎回ちょうど1減る → 高々 n-1 回）に統一し、
  `reference/two-pointers-template.html` §4 を差し替え。**償却の3文は sliding window 専用に戻す。**
  課題：167 の⑥③を書き直す（コードは触らない）→ 11 は③を一文書いてから実装。
- **誤りの型が判明：「知らない」ではなく「隣の欄に書く」**（3回連続）。
  「捨てる」→⑥に漏れて O(logn)／②の文を③欄に／ギャップ論法を [時間2] に。
  → `interview-protocol.html` の rubric に**形式チェック**を追加。内容ではなく**文の形**で弾く：
  [時間2] は必ず `O(…)` で終わる／② は「動かす」で終わる動作文／**③ は「…ない」で終わる否定文**。
  さらに③の**定型文（穴埋め）**を用意。**枠を与えれば埋まる**（LR-0009）の再適用で、2往復で解決。
- 167 完了。⑥は3欄とも正答。③は定型文で骨格が通ったが、**2文目で X と Y が入れ替わった**。
  2文目は「1組の事実 → 全組の結論」の唯一の橋渡し＝面接官が踏み込む場所。書き間違いと見て軽く指摘。
  **一般化の段だけが壊れる**現象は要観察（1文目・3文目は正しかった）。
- **11 完答。ただし7往復**（sliding window は1〜2往復だった）＝現在の最難関。詳細 LR-0012。
  詰まりは5種類で、核心は**「ソート済みでないから捨てられない」**。167 から「③にはソートが必要」と過剰般化。
  → **③は値を知ることではなく上限を押さえること。**11 の上限は `min` と添字から出るのでソート不要
  （`h[j]` は最後まで一度も要らない）。**ソートは③の必須条件ではなく、上限を得る手段のひとつ。**
  また③の主張は「捨てた中に良いものがない」ではなく「**捨てたものは既に計算した値を超えられない**」。
- **③は具体例で書かせない。**`h[0]` で書いたため適用条件（＝捨てるのは低いほう）が文面から落ち、
  コードが `left += 1` 無条件になった。**③の具体例化とコードのバグは同一の欠落。**
  逆に**③が一般形で書ければ、コードの分岐は自動的に決まる**。次問でこの対応を明示的に確認する。
- **定型文（穴埋め）は形を与えるが中身を保証しない。**167 の定型文がそのまま流用された。
  効いたのは定型文ではなく「問1=幅／問2=水位／問3=面積」と**問題固有の量を先に個別に問う**やり方。
  枠は「問題固有の量を要求する形」でないと空虚に埋まる（LR-0009 の限界）。
- **`⚠` に3回連続で `left < right`。**167 で解決済みなので摩耗している。
  毎回**その問題固有の行**を指させること（11 なら `if height[left] < height[right]` の同値時の扱い）。
  → 11・125 とも直後に修正され、**4回連続で的中**（125 は内側 while の `l < r` ガード）。

## 125 / two pointers 3問通過（2026-07-22、LR-0013）
- **③には2種類ある**（テンプレートの欠落。3往復の原因）。
  **優越型**（167/11）＝捨てた候補は手元の値を超えられない／**縮小型**（125）＝問題が同じ形のまま小さくなる。
  125 は探索ではなく**検証**なので②（どちらを動かすか）が存在せず、③も上限ではなく**同値変形**になる。
  「対称性から上限が出る」と誤って振ったのは教える側のミス。訂正済み、rubric にも2型を明記。
- **③で結論だけ残して理由を落とす癖**（3回連続）。弁別規則を渡した：
  **「〜する」で終わったら②（手順）。③は「〜と同じ」「〜に影響しない」「〜ない」で終わる。**
- **`[時間2]` の misfile が3回目**（167 `right-left`／125 `n回`＝どちらも量をコスト欄に）。
  形式チェックでは防げなかったので、**自己検算式**に落とした：
  `[時間1] × [時間2] + [時間3]` を計算して結論と突き合わせる（`n/2 × n回 = O(n²)` で矛盾が露見）。
  **警告ではなく矛盾が自動的に出る手続きに変える**——次問 15 でこの介入を検証する。
- **主張と実装の不一致**（新規）：②に「O(n) メモリを避けた」と書きつつ `s = s.lower()` が残っていた。
  **②⑥で主張したことが実装と一致しているかの照合が抜けている。**
- **学習者が rubric を修正した**：「何の再計算を避けたか」→「何を避けたか」。
  125 のボトルネックは時間ではなく空間なので正しい判断。採用。
  **枠を埋める段階から、枠の妥当性を判断する段階に入った兆候。**

## 教える側への教訓（重要・再発中）
- **抽象化した箇所が、その問題の難所である可能性を常に疑う。** `is_invalid` / `add` / `remove` のような
  プレースホルダは、易しい問題では自明に埋まるので欠陥が見えない。**難しい問題で初めて露呈する。**
- LR-0002（助走問題の選定ミス）と同じ構図の再発。**資料は必ず「一段難しい問題」で検証してから渡す。**
- 本人は骨格を全て正しく書けている。詰まりを1点に絞れているときは、**そこだけを教える**（周辺を復習しない）。
- **コードが通ったことを理解の証拠にしない。**424 は正しく書けたのに②に誤った模型が残っていた（LR-0008）。
  実装の正しさと言語化の正しさは独立に確認する。ミッション上、後者のほうが配点が高い。
- レッスンで**名指しで警告した点が翌回に落ちる**ことがある（O(26) の言及）。
  警告は知識にはなるが習慣にはならない。習慣にしたいものは**チェックリスト化して提出物に埋め込む**。
- **コードの指摘は直すが、説明の指摘は直さない**（LR-0012、2往復とも未反映）。
  → ただし「**ミッション上こちらのほうが配点が高い**」と一度明示したら、次の提出で3箇所とも直った。
  **気づけないのでも拒否でもなく、優先度の問題。**説明の指摘には毎回「なぜ直す価値があるか」を一言添える。
- **前問の文面をそのまま持ち込む癖**（③で1回、⑥[時間2]の「加算と比較」で1回）。
  11 に加算はないのに167の文が残っていた。提出前チェックとして「前の問題の文が混ざっていないか」を渡した。
- **例の付随的特徴が概念に貼り付く**（3回目）。167 のソート → 「③にはソートが必要」。
  Lesson 07 の「捨てる」→ log n。**例を出したら、その例に固有な部分と一般な部分を必ず切り分けて言う。**
- **強調した語彙は、隣接する誤概念を引き寄せる**（LR-0011）。「捨てる」を核心語にした結果、
  二分探索の「半分捨てる → log n」が⑥に漏れた。資料に正解が明記してあっても上書きされる。
  → **キーワードを立てるときは、その語が既に結びついている別の型を必ず並べて否定する。**

## 観察された傾向
- 「毎回 brute force を考えるべきか」と自分から質問（2026-07-19）。**面接の進め方への関心が出てきた**のは good sign。
  `reference/interview-protocol.html` を作成。以後の演習指示には「②素朴解を声に出す」「⑥計算量を総括する」を
  毎回入れて、台本を演習に埋め込む。模擬面接（8月下旬予定）への地ならしになる。
- **専門用語を説明なしに使わない。**「単調」「償却論法」で詰まらせた（2026-07-19）。
  CS用語の日本語訳は、意味が自明に見えて実は共有されていない。初出時は必ず一行で定義し、
  `reference/glossary.html` に追加する。用語集は作成済み、以後これに揃える。
- **選択式クイズが通っても実装は通らない**（LR-0002）。概念の確認は必ず
  「コードを書かせる／穴埋め」の形で取ること。以後のレッスン設計の必須要件とする。
- 助走問題は、教えたテンプレートが**そのまま動く**問題に限る。反例を混ぜるのは定着後（LR-0002）。
- **真犯人は「境界指定」**（LR-0004）。1セッションで3回のオフバイワン。一方アルゴリズムでは一度も詰まっていない。
  概念 ≫ 境界の非対称。以後、レッスンの重心を「パターンの説明」から「境界の検算習慣」に寄せる。
  与えた道具＝**小さい具体値を代入して数える**（k=2, nums=[1,2,3]）。次回冒頭で定着を確認すること。
- **発話の曖昧さがバグ位置を予測した**（LR-0004）。②（素朴解の口頭化）は説明力の訓練であると同時に、
  実装前のバグ検出器として実際に機能している。継続する。
- パターンの選択とキーの着想は自力でできる。**詰まるのは言語仕様の側**（2セッション連続）。
  → レッスンには毎回「この型でよく踏む Python の地雷」を一つ添えると効率が良い。
- 詰まっても答えを見ずに別解（26要素の配列 → tuple）に回り込んだ。粘り強さがあるので、
  ヒントは早出しせず、問いの形で投げるほうが機能する。
