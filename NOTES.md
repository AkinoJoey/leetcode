# NOTES

## 学習者プロフィール（2026-07-19 初回セッションで確認）
- ゴール：技術面接の突破（短期集中、1〜3ヶ月）
- 現在地：Easy は解ける／Medium で詰まる
- 言語：Python
- 出力言語：日本語（`CLAUDE.md` の指定）

## 教え方の方針
- ミッションが「面接突破」なので、常に**口頭で説明できる形**まで持っていく。
  計算量とトレードオフを言語化させることを毎レッスンに埋め込む。
- Medium で詰まる人の典型は「素朴解は出るが最適化に進めない」。
  レッスンは「パターン名 → 見分け方の合図 → テンプレート」の順で固定する。
- NeetCode Roadmap の依存順を進行の骨格にする。ただし1レッスン＝1トピックの一部に絞る。

## 確認済み（2026-07-19）
- 面接は **2026年10月以降**。準備期間 = 約10週間（〜09-30）
- 使える時間 = 平日1h／週末2〜3h → **週約10時間、総計約100時間**
- 模擬面接の経験なし。抵抗の有無は未確認だが、未経験であることは確定。

## 未確認・次回聞くこと
- 志望企業（分かれば頻出問題の重み付けができる）
- 模擬面接そのものへの心理的な抵抗の有無（未経験なだけか、避けたいのか）

## 計画上の判断
- 100時間では NeetCode 150 の完走は非現実的。**Blind 75 相当のコアを確実に**取り、
  余力でパターンごとの2問目・3問目に広げる方針とする。
- 最大リスクは技術力ではなく**口頭説明の未経験**。9月に入る前（8月下旬・50問時点）に
  初回の模擬面接を必ず入れる。ここは後回しにさせない。
- 週の設計：平日=新規パターン学習（1h＝レッスン+1問）、週末=復習と多問演習（interleaving）。

## 進行ログ
- Lesson 01: ハッシュによる O(n²) → O(n) 変換。**Group Anagrams を独力で完答**（LR-0001）。
  詰まりどころはアルゴリズムではなく Python のハッシュ可能性だった。
- Lesson 02: sliding window（可変長・最長型）。冒頭に LR-0001 で未確認だった
  「キー設計を計算量で比較する」観点を retrieval quiz として仕込んだ（spacing）。
  演習：Best Time to Buy and Sell Stock → Longest Substring Without Repeating → Character Replacement。
  → 結果：**Maximum Average Subarray I を自力で解けなかった**（解答を見て理解）。可変長窓の2問は未着手。
  LR-0004 の「定着していなければレッスンを1本これに割く」条件に該当したため、Lesson 03 を境界に全振りした。
- Lesson 03: 境界の検算に一本化。新パターンなし。代入検算（小さい具体値を手で数える）を手続きとして与え、
  ドリル6問＋ Maximum Average Subarray I の穴埋め3箇所（すべて境界）で確認。
  LR-0002 の教訓に従い**選択式を一切使わず記述式のみ**にした（`assets/input-check.js` を新規作成）。
  `reference/boundary-checklist.html` を新設。課題：643 を白紙から再実装 → 3（可変長窓）。
  → **643 を自力で完答**（LR-0005）。可変長窓（3）は未着手、次セッションの入口。

## 運用ルール（2026-07-20 合意）
- 演習コードの冒頭に **②素朴解 / ⑥計算量 / ⚠言えなかったこと** をコメントで記載して提出する。
  書式は `reference/interview-protocol.html` の「練習時の記録フォーマット」節。
- 順序厳守：**口に出す → 言えたことだけ書く**。補完して書かせない（推敲が入ると②が検出器として死ぬ）。
- `⚠ 言えなかったこと` を毎回読むこと。**ここが次のレッスンの題材になる。**
- ②のコメントは模擬面接（8月下旬）以降に外す。⑥は継続。
- **`⚠ 言えなかったこと` → `⚠ いちばん自信のない行` に変更（2026-07-20、LR-0006 を受けて）。**
  「なし」禁止。自覚できない誤りは自己申告欄に書けないため、必ず1つ出させる形式にする。

## 進行ログ（続き）
- Lesson 04: 償却論法。3（Longest Substring）はテンプレート参照で完答、境界エラーはゼロ。
  ただし⑥の根拠が「各要素は最大**1回**」と誤っており、かつ `⚠ なし` と自己申告（LR-0006）。
  → 「入るとき1回・出るとき1回＝2回」を `s="abba"` のトレースで数えさせ、**3文テンプレート**を与えた。
  この型は two pointers・単調スタックで再利用するので、以後の計算量説明はこの3文に統一する。
  空間の粒度（`O(min(n, 文字種))`）も併せて指導。
  課題：424 Character Replacement を**まず10分自力で**（テンプレートを開いたら正直に申告させる）。
- Lesson 05: `is_invalid` の導出。424 は**テンプレートを見ても解けなかった**（LR-0007）。
  原因は本人ではなく**資料側**：可変長テンプレートが `while is_invalid(state):` と抽象化し、
  424 の唯一の難所を関数名で隠していた。3 では valid が問題文に直書きだったため露呈しなかった。
  → 「**日本語 → 量 → 不等号**」の3段翻訳を明示。テンプレートに導出節と問題別の対応表を追記。
  課題：424 を白紙から（`# valid とは:` を最初の行に必須）→ 1004 Max Consecutive Ones III で同じ導出を反復。

- Lesson 06: ②⑥の精度。424 は白紙から自力完答（`is_invalid` の導出は1回で定着）。
  一方 **2セッション連続で⑥に誤り**（LR-0008）：空間 `O(min(1,26))`＝形だけの流用、
  `max(counter.values())` の O(26) 走査に言及なし（前回名指しで警告した箇所）、
  ②の素朴解に「最頻文字」が登場しない＝**詰まったときの誤った模型が②に残存**。
  → ⑥を自由記述から **rubric 付き**に変更（`reference/interview-protocol.html` に採点表）。
  空間は idiom 暗記をやめ「状態 → 最大要素数 → 入力依存か」の3段導出に。
  ②の検算法：**最適解の主役が素朴解に登場しているか**を確認する。
  課題：424 の⑥を rubric で書き直す → 1004。
- **`⚠ いちばん自信のない行` は初回から機能した**（挙げたのは `while` の条件式＝核心）。形式変更は成功、維持。
- rubric 化は成功（LR-0009）。3往復で②⑥が全項目クリア。空間は枠を与えただけで一発正解。
  時間の枠は [時間2]（毎回走るコスト）/[時間3]（償却で数える内側ループ）に**分割済み**。以後この形で運用。
  残る弱点は**ボトルネックの言語化**。「重なった部分を数え直している」は自力で出ず、今回は与えた。
  → 次に window 型を解くとき、この一文を言えるか再確認する（storage strength の検査）。
- 1004 を**10分で自力完答**。sliding window は4問通過、**獲得済みとみなして two pointers に移行**。
  `⚠` に `while 0 in counter and counter[0] > k` を挙げたのが的確（`defaultdict` のキー生成防御＝唯一議論の余地がある行）。
- Lesson 07: Two Pointers。難所は骨格ではなく **③「捨ててよい理由」**（＝この型における `is_invalid`）。
  LR-0007 の教訓により、テンプレートで抽象化せず**最初から開示**し、問題別の対応表も作った
  （`reference/two-pointers-template.html`）。計算量は Lesson 04 の3文がそのまま再利用できる点を強調。
  課題：167（助走・骨格がそのまま動く）→ 11 Container With Most Water（③が本番）。
- **rubric 更新（2回目）**：ボトルネック欄を「**何の再計算を避けたか**」に文言変更（LR-0010）。
  two pointers 用に「③ 捨ててよい理由」欄を追加。
- 状態の選択（1004 で int で足りるのに `defaultdict`）は未獲得。減点せず一行だけ触れた。次回以降も軽く反復。
- **167 を自力完答**（境界・1-indexed とも正しい）。`⚠` は `left < right`＝3回連続で急所を的中。
  一方 ⑥ で **`[時間1] logn回` → O(logn)** と誤答（LR-0011）。資料に O(n) と明記済みなのに上書きされた
  ＝**Lesson 07 が「捨てる」を核心語に据えたことで二分探索の模型を引き寄せた**（資料起因）。
  ③も2段目止まり（3段目＝捨てる側に良い答えが残っていない理由が欠落）。**対話では言えたのに書くと落ちる**。
- Lesson 08: ③と⑥を**一体で**扱う。③＝なぜ捨てるか（正しさ）／⑥＝1回でどれだけ捨てるか（速さ）。
  「log n が出てよいのは捨てる量が**残りの一定割合**のときだけ」を対比表で明示。
  two pointers の計算量説明を**ギャップ論法**（`right-left` が毎回ちょうど1減る → 高々 n-1 回）に統一し、
  `reference/two-pointers-template.html` §4 を差し替え。**償却の3文は sliding window 専用に戻す。**
  課題：167 の⑥③を書き直す（コードは触らない）→ 11 は③を一文書いてから実装。
- **誤りの型が判明：「知らない」ではなく「隣の欄に書く」**（3回連続）。
  「捨てる」→⑥に漏れて O(logn)／②の文を③欄に／ギャップ論法を [時間2] に。
  → `interview-protocol.html` の rubric に**形式チェック**を追加。内容ではなく**文の形**で弾く：
  [時間2] は必ず `O(…)` で終わる／② は「動かす」で終わる動作文／**③ は「…ない」で終わる否定文**。
  さらに③の**定型文（穴埋め）**を用意。**枠を与えれば埋まる**（LR-0009）の再適用で、2往復で解決。
- 167 完了。⑥は3欄とも正答。③は定型文で骨格が通ったが、**2文目で X と Y が入れ替わった**。
  2文目は「1組の事実 → 全組の結論」の唯一の橋渡し＝面接官が踏み込む場所。書き間違いと見て軽く指摘。
  **一般化の段だけが壊れる**現象は要観察（1文目・3文目は正しかった）。
- **11 完答。ただし7往復**（sliding window は1〜2往復だった）＝現在の最難関。詳細 LR-0012。
  詰まりは5種類で、核心は**「ソート済みでないから捨てられない」**。167 から「③にはソートが必要」と過剰般化。
  → **③は値を知ることではなく上限を押さえること。**11 の上限は `min` と添字から出るのでソート不要
  （`h[j]` は最後まで一度も要らない）。**ソートは③の必須条件ではなく、上限を得る手段のひとつ。**
  また③の主張は「捨てた中に良いものがない」ではなく「**捨てたものは既に計算した値を超えられない**」。
- **③は具体例で書かせない。**`h[0]` で書いたため適用条件（＝捨てるのは低いほう）が文面から落ち、
  コードが `left += 1` 無条件になった。**③の具体例化とコードのバグは同一の欠落。**
  逆に**③が一般形で書ければ、コードの分岐は自動的に決まる**。次問でこの対応を明示的に確認する。
- **定型文（穴埋め）は形を与えるが中身を保証しない。**167 の定型文がそのまま流用された。
  効いたのは定型文ではなく「問1=幅／問2=水位／問3=面積」と**問題固有の量を先に個別に問う**やり方。
  枠は「問題固有の量を要求する形」でないと空虚に埋まる（LR-0009 の限界）。
- **`⚠` に3回連続で `left < right`。**167 で解決済みなので摩耗している。
  毎回**その問題固有の行**を指させること（11 なら `if height[left] < height[right]` の同値時の扱い）。
  → 11・125 とも直後に修正され、**4回連続で的中**（125 は内側 while の `l < r` ガード）。

## 125 / two pointers 3問通過（2026-07-22、LR-0013）
- **③には2種類ある**（テンプレートの欠落。3往復の原因）。
  **優越型**（167/11）＝捨てた候補は手元の値を超えられない／**縮小型**（125）＝問題が同じ形のまま小さくなる。
  125 は探索ではなく**検証**なので②（どちらを動かすか）が存在せず、③も上限ではなく**同値変形**になる。
  「対称性から上限が出る」と誤って振ったのは教える側のミス。訂正済み、rubric にも2型を明記。
- **③で結論だけ残して理由を落とす癖**（3回連続）。弁別規則を渡した：
  **「〜する」で終わったら②（手順）。③は「〜と同じ」「〜に影響しない」「〜ない」で終わる。**
- **`[時間2]` の misfile が3回目**（167 `right-left`／125 `n回`＝どちらも量をコスト欄に）。
  形式チェックでは防げなかったので、**自己検算式**に落とした：
  `[時間1] × [時間2] + [時間3]` を計算して結論と突き合わせる（`n/2 × n回 = O(n²)` で矛盾が露見）。
  **警告ではなく矛盾が自動的に出る手続きに変える**——次問 15 でこの介入を検証する。
- **主張と実装の不一致**（新規）：②に「O(n) メモリを避けた」と書きつつ `s = s.lower()` が残っていた。
  **②⑥で主張したことが実装と一致しているかの照合が抜けている。**
- **学習者が rubric を修正した**：「何の再計算を避けたか」→「何を避けたか」。
  125 のボトルネックは時間ではなく空間なので正しい判断。採用。
  **枠を埋める段階から、枠の妥当性を判断する段階に入った兆候。**

## 教える側への教訓（重要・再発中）
- **抽象化した箇所が、その問題の難所である可能性を常に疑う。** `is_invalid` / `add` / `remove` のような
  プレースホルダは、易しい問題では自明に埋まるので欠陥が見えない。**難しい問題で初めて露呈する。**
- LR-0002（助走問題の選定ミス）と同じ構図の再発。**資料は必ず「一段難しい問題」で検証してから渡す。**
- 本人は骨格を全て正しく書けている。詰まりを1点に絞れているときは、**そこだけを教える**（周辺を復習しない）。
- **コードが通ったことを理解の証拠にしない。**424 は正しく書けたのに②に誤った模型が残っていた（LR-0008）。
  実装の正しさと言語化の正しさは独立に確認する。ミッション上、後者のほうが配点が高い。
- レッスンで**名指しで警告した点が翌回に落ちる**ことがある（O(26) の言及）。
  警告は知識にはなるが習慣にはならない。習慣にしたいものは**チェックリスト化して提出物に埋め込む**。
- **コードの指摘は直すが、説明の指摘は直さない**（LR-0012、2往復とも未反映）。
  → ただし「**ミッション上こちらのほうが配点が高い**」と一度明示したら、次の提出で3箇所とも直った。
  **気づけないのでも拒否でもなく、優先度の問題。**説明の指摘には毎回「なぜ直す価値があるか」を一言添える。
- **前問の文面をそのまま持ち込む癖**（③で1回、⑥[時間2]の「加算と比較」で1回）。
  11 に加算はないのに167の文が残っていた。提出前チェックとして「前の問題の文が混ざっていないか」を渡した。
- **例の付随的特徴が概念に貼り付く**（3回目）。167 のソート → 「③にはソートが必要」。
  Lesson 07 の「捨てる」→ log n。**例を出したら、その例に固有な部分と一般な部分を必ず切り分けて言う。**
- **強調した語彙は、隣接する誤概念を引き寄せる**（LR-0011）。「捨てる」を核心語にした結果、
  二分探索の「半分捨てる → log n」が⑥に漏れた。資料に正解が明記してあっても上書きされる。
  → **キーワードを立てるときは、その語が既に結びついている別の型を必ず並べて否定する。**

## 観察された傾向
- 「毎回 brute force を考えるべきか」と自分から質問（2026-07-19）。**面接の進め方への関心が出てきた**のは good sign。
  `reference/interview-protocol.html` を作成。以後の演習指示には「②素朴解を声に出す」「⑥計算量を総括する」を
  毎回入れて、台本を演習に埋め込む。模擬面接（8月下旬予定）への地ならしになる。
- **専門用語を説明なしに使わない。**「単調」「償却論法」で詰まらせた（2026-07-19）。
  CS用語の日本語訳は、意味が自明に見えて実は共有されていない。初出時は必ず一行で定義し、
  `reference/glossary.html` に追加する。用語集は作成済み、以後これに揃える。
- **選択式クイズが通っても実装は通らない**（LR-0002）。概念の確認は必ず
  「コードを書かせる／穴埋め」の形で取ること。以後のレッスン設計の必須要件とする。
- 助走問題は、教えたテンプレートが**そのまま動く**問題に限る。反例を混ぜるのは定着後（LR-0002）。
- **真犯人は「境界指定」**（LR-0004）。1セッションで3回のオフバイワン。一方アルゴリズムでは一度も詰まっていない。
  概念 ≫ 境界の非対称。以後、レッスンの重心を「パターンの説明」から「境界の検算習慣」に寄せる。
  与えた道具＝**小さい具体値を代入して数える**（k=2, nums=[1,2,3]）。次回冒頭で定着を確認すること。
- **発話の曖昧さがバグ位置を予測した**（LR-0004）。②（素朴解の口頭化）は説明力の訓練であると同時に、
  実装前のバグ検出器として実際に機能している。継続する。
- パターンの選択とキーの着想は自力でできる。**詰まるのは言語仕様の側**（2セッション連続）。
  → レッスンには毎回「この型でよく踏む Python の地雷」を一つ添えると効率が良い。
- 詰まっても答えを見ずに別解（26要素の配列 → tuple）に回り込んだ。粘り強さがあるので、
  ヒントは早出しせず、問いの形で投げるほうが機能する。

## 15. 3Sum 未完（2026-07-23、LR-0014）
- 骨格は完全に正解（sort → i 固定 → 両端）。**two pointers の型そのものは獲得済み**とみなす。
  落ちたのは2点：`break` による解の取りこぼしと、⑥ O(n)（正しくは O(n²)）。
- Lesson 09: **15 は「167 を n 回解く」帰着**として一本化。この見方から両方の誤りが同時に落ちる。
  - コスト＝掛け算（内側 `j,k` が毎回リセットされるので償却できない）
  - `break` ＝部分問題を解ききっていない。**内側ループの止め方は外側の問題文が決める**
    （167 は「1つ返せ」→ return 可／15 は「全部返せ」→ 続行）。
- **自己検算式（LR-0013 の介入）は失敗した。** `n × O(1) + O(n) = O(n)` で矛盾が出ない。
  原因は**式ではなく欄名**：`[時間3] 回るとは限らない内側ループ（償却）` が償却を前提していた。
  → 欄名を「**持ち越しかリセットかを明記**」に変更。前提付きの検算式は前提ごと渡す。
- **教訓（追加）：抽象化だけでなく、欄名に埋め込んだ前提も同じ罠になる。**
  資料の枠は「その問題では自明に成り立つ前提」を含みやすく、一段難しい問題で初めて壊れる。
- **`break` は 167 の文面の持ち込み。**「前問の文面を持ち込む癖」が docstring だけでなく
  **コードで起きたのは初めて**。演習指示に「前問と何が変わったか」を一行書かせるのが次の手か（未導入）。
- **空間欄の自己矛盾**（新規の型）：`[空間2] O(1)` かつ `[空間3] する`。
  2欄は同値（定数個 ⟺ 依存しない）。**枠が複数あると、枠同士の矛盾は見逃される。**
  rubric に整合チェックを追加。`nums.sort()` の計上漏れも同種。
- ③は形式チェックを通過（否定文）。125 の3往復から明確に改善。残るのは3段目が結論だけの点。
- **`⚠` 5回連続的中**（`for i in range(len(nums))` ＝ break と重複スキップの所在）。
- 課題：15 を直して通す（重複スキップ版も）→ 16. 3Sum Closest。
  16 では「target 一致で return してよいか」を外側の問題文から判断できるかを見る。
- 15 再提出（同日）：`break` は解消したが、**重複スキップの `while` を外側 while の条件に `and` で合流**させ、
  探索ループがほぼ回らなくなった（`[-3,-1,0,1,2,3] → []`）。**論理ではなく配置の誤り。**
  原因は資料側：Lesson 09 の課題欄で断片を `...` で示し、**ネストの文脈を落として**渡した
  （reference §4.5 には全体像があったが、学習者は課題欄の断片を使った）。
  → **断片は必ず入れ子の外側ごと示す**。Lesson 09 修正済み（`while j < k` が2つあり役割が違う点を明記）。
- **`⚠` 6回連続的中**（`while j < k and nums[j] == nums[j-1]` ＝まさに壊れた行）。
- **空間欄（[空間2]/[空間3] の矛盾）は名指しで指摘したのに未修正**。②③⑥のうち直したのは③と時間のみ。
  LR-0012 の「説明の指摘は直さない」の再発だが、今回は**時間は直して空間だけ残した**＝
  優先度ではなく**指摘の粒度**の問題の可能性。次回は空間だけを単独の課題として切り出す。

## 16. 3Sum Closest 未完（2026-07-25、LR-0015）
- **欠落は `nums.sort()` の1行のみ。**ロジックにバグなし（7458件のランダムテストで sort を足すだけで全通過。
  提出版は35%失敗）。40分溶けたのは**探す場所が違った**から。
- **前提はコードに書かれていないので、最後まで疑われない。**しかも 16 は前提を落としても落ちず、
  それらしい数字を返す。→ デバッグ規則：**ロジックより先に前提を疑う**（確認10秒 vs 追跡40分）。
- 解決：**③を一般形で書くと前提が文の中に現れる**（「**ソート順より** `nums[k'] ≤ nums[k]` なので…」）。
  LR-0012 の「③が書ければ**分岐**が決まる」の続き＝「③が書ければ**前処理**も決まる」。
  **③は正しさの説明であると同時に実装の生成規則**として2回連続で機能。
- **⓪前提 の欄を新設**（`interview-protocol.html` の docstring・rubric 両方）。必ず2行：
  前提／**それを保証しているコードの行の名指し**。名指しできなければその行はまだ存在しない。
  167 は「—（問題文が保証）」と書ける＝問題文が保証する場合と自分で書く場合を同じ欄で弁別できる。
- **合格点：`return three_sum` は正解**（LR-0014 検証点(b)）。15 の `break` と同じ判断を問題文から正しく引き直した。
  ⑥は未提出なので検証点(a)（`[時間3]` に掛け算）は**未回収**、次回持ち越し。

### 教える側への教訓（追加・重要）
- **教材の否定形は、肯定形とペアで置く。** LR-0012 で 11 の詰まりを解くために
  「**ソートは③の必須条件ではない**」を強く打ち出したが、**逆方向（では、いつ要るのか）を言わなかった**。
  15 では書けていた `sort()` が 16 で落ちた＝「知らない」ではなく**一度打ち消された**と見るのが自然。
  NOTES 既出の「強調した語彙は隣接する誤概念を引き寄せる」の新形——今回引き寄せたのは**語彙ではなく否定**。
  → `two-pointers-template.html` を「ソートは必須ではない」→
  **「上限はどこから来るか ＝ ソートが要るかの判定」**に改題し、要る／要らないを同じ枠に並置。
  判定は一文：**③が「値の大小」に依存しているならソートが要る。添字や対称性で済むなら要らない。**
- **同じ `sort()` でも問題ごとに理由が違う**（15＝単調性＋重複スキップ／16＝単調性のみ）。
  「前問の文面を持ち込む癖」の予防として、理由の側を毎回問うこと。
- Lesson 10 の検証点：16 再提出で（a）⓪が2行で埋まるか（b）`[時間3]` にリセット＝掛け算と書けるか
  （c）`sort()` により空間が `O(1)` にならないと気づけるか。**(c) は 15 で名指ししたまま唯一未修正の欄。**

## 提出形式の変更：枠を採点側に移す（2026-07-25、学習者の提案）
- 学習者から提案：**「実装前／実装後に分けて、自由形式で回答する」**。理由は「より実践に近く、ヒントにもならない」。
  **採用。**私が用意した3案（docstring 継続／私が質問／折衷）より優れている。
- **枠（rubric）は廃止しない。提出物から採点側へ移す。**学習者は白紙で答え、私が rubric と突き合わせて
  「出てこなかった項目」を返す。その場でヒントは出さない。
- 優れている点：
  1. **枠は完全性を保証すると同時に捏造する。**欄があるから埋まっていただけの項目が、白紙では露出する
     （LR-0009「枠を与えれば埋まる」の限界＝ fluency strength を測っていた可能性）。
  2. **⓪前提を実装「前」に言わせられる。**LR-0015 の40分はこれで防げた。事後に書かせても再現しない。
  3. **実装後の回答をコードと照合できる**（LR-0013「主張と実装の不一致」の検出）。
- 運用：私からは**枠も項目名も出さない**。面接官が言う程度の短い問い（「計算量は？」「前提は？」「弱い行は？」）だけ。
- **16 は実装前パートを取れない**（既に解けているため）。実装後のみ実施し、**18 から完全版**。
- リスク：15セッション分の足場を一度に外すことになる。**次の2問で「項目の脱落」が急増しないか観察する。**
  急増したら、枠を戻すのではなく**実装前パートだけ項目名を出す**（実装後は白紙のまま）で段階を刻む。
- なお `interview-protocol.html` の「模擬面接（8月下旬）以降に口頭へ戻す」は**前倒しで実質達成**。
  ⓪前提の欄（LR-0015 で新設）は docstring からではなく**採点表の項目として**生き残る。

## 16 完答（2026-07-25、LR-0016）／新形式の初回
- **⑥の根拠が3回連続で未回収**（15 → 16 docstring → 16 追い質問）。結論 O(n²) は正しいが根拠が空で、
  2回とも **424 にも当てはまってしまう説明**（「外側と内側でループが回る」「外側の中でポインタを動かす」）。
- **原因は教材（3回目の同型）。**「持ち越されるか／リセットされるか」は正しいが**追跡しないと判定できない**。
  → **意味判定を構文の観察に置き換えた**：
  **内側ポインタの初期化行が外側ループの「外」なら足し算、「中」なら掛け算。**
  `424: left = 0`（外）／`16: j,k = i+1, n-1`（中）。**インデントを見るだけ。**
  前提（内側ポインタの単調性＝LR-0002）は Lesson 11 のクイズで明示的に切り出した。
- **教訓（新規・重要）：抽象度の高い判定基準は、3回試して出なければ構文の観察に落とす。**
  「警告は知識になるが習慣にならない」の解決形が一つ増えた——
  **チェックリスト化できないなら、目で見えるものに変換する。**
- 回収できたもの：**空間 O(n) を根拠つきで自力**（15 から唯一未修正だった欄、2セッション越し）／
  前提「length ≥ 3」（私の想定と違うが**本物の前提**。`[1,2]` で例外を出さず `0` を返す）／**`⚠` 7回連続的中**。
- `⚠` の中身：`curr_diff` の**式**は言えたが**時制**が抜けた（その行の時点で j,k は既に動いている）。
  結果は正しい（移動前に確定させている）。より安全な形＝更新を移動の前に置く、を提示。

### 新形式（実装前／実装後・自由記述）の初回所見
- **項目の脱落は急増しなかった。**落ちたのは枠がある時代にも落ちていた項目のみ。**枠の撤去による劣化は未観測。**
- **むしろ空間は枠がないほうが良く書けた。**`[空間1][空間2][空間3]` の3欄は互いに矛盾する事故を生んでいたが
  （LR-0014）、自由記述では一文で整合的に出た。**枠が誤りを作っていた可能性。**
- **私の問いの文言ミス。**「**入力について**何を仮定しているか」と聞いたため、自分で `sort()` している以上
  ソートは入力への仮定ではない、という正しい解釈で答えが逸れた。
  → 以後「**この解法が成り立つために必要な条件と、それを保証しているのは誰か（問題文か、あなたのコードか）**」。
- 次の検証点：18. 4Sum で**インデント判定により** `O(n³)` と言えるか（内側2重なので判定法を2回適用する構造）。

## 18. 4Sum 実装前パート（2026-07-25、LR-0017）
- **⓪前提は満点。**「ソート済みであること。引数で受け取った時点ではソート済みではないため、関数内でソートして保証する」
  ＝ **条件／保証しているのは誰か（問題文かコードか）** が両方。Lesson 10 の形がそのまま出た。**獲得済みとみなす。**
- **③が5回連続で②になる。**原因を特定：**主語**。②の主語はポインタ、**③の主語は消えた組**。
  5回の答案すべてで**消えた組が一度も名指しされていない**。定型文が `捨てる X は…` で始まるのに
  **X が空欄のまま文だけ完成**するので、形式チェックも弁別規則も素通りする。
  → **教訓：形式規則は文の形を検査するが、主語は検査しない。**
  新しい説明項目を導入するときは**最初に「この文の主語は何か」を言う。**
- **決定的な観察**：「`d -= 1` で二度と評価されなくなる組は？」→「`nums[c]+nums[d]`」（＝評価済みの組）。
  **未評価のまま失われる組と、評価済みの組の区別がついていない。**
  これは 11 で記録した詰まり（LR-0012 の詰まり3）と**完全に同一**。
  **11 で効いたペア空間の絵を、11 限りの説明として扱い一般化しなかったのが直接の失敗。**
  → **一度効いた図は reference に格上げする。**
- Lesson 12：ペア空間の格子を常設装置に。**`d -= 1` は列を消す／`c += 1` は行を消す。消えるのは未評価側だけ。**
  ③はこの絵から4行で読める。「和を小さくできる」は一度も登場しない。
  `two-pointers-template.html` に「③を書く手順（先に2つ名指しする）」を追加。
- **LR-0016 と同じ処方が2回連続で効いた：抽象的な基準は、目で見えるものに変換する。**
  「持ち越しか」→ インデント／「捨ててよい理由」→ 格子から消える列。**第一選択の介入とみなす。**
- ②：素朴解 `O(n⁴)` は正答、**ボトルネックは2回連続で欠落**（要観察）。
- 方針：「2本固定・2本が両端」は正しいが、**`while a < d` の単一ループ**と書いた
  ＝ `(a,d)` の組が n 組しか回らない。計算量の自己検算（O(n²) は素朴解 O(n⁴) と比べて速すぎる）で露見。
- **前問の文面を持ち込む癖、4例目**：問3に「近くなった」（16 の語彙。18 は closest ではなく厳密一致）。
  ③・コード・docstring に続き、**質問への回答でも**出た。
- **Lesson 12（格子）は一発で効いた。**絵を見た直後、③の主語が正しくなった
  （「(c+1.., d) の組み合わせは消える」＝消えた組を**集合で**名指し／根拠はソート順／「小さくできる」が消滅）。
  5回外していた場所を1往復で越えた。**「抽象的な基準を目で見えるものに変換する」の3連勝。**
- **誤りの向きが反転した。**従来は「結論だけ残して理由が落ちる」（3回記録）→ 今回は**理由が揃って結論が落ちた**。
  残りは「ゆえに target に一致しない」の一文と、範囲の上端（`c+1..n-1` → `c+1..d-1`）のみ。

## 18. 4Sum 完答（2026-07-25、LR-0018）— two pointers 6問通過、型は獲得済み
- **⑥の根拠を初めて回収**：「二重ループの**中で**ポインタを毎回初期化しているため O(n³)」＝
  Lesson 11 の判定法が結論ではなく**根拠として**出た。3回連続で外していた項目を1回で。
  **「抽象的な基準は目で見えるものに変換する」が3勝目**（インデント→⑥／格子→③）。**第一選択の介入として確立。**
- バグは1箇所 `b > 1`（正しくは `b > a+1`）。ランダム4000件中 358件失敗、**LeetCode の例題は通る**。
  → **規則：重複スキップの境界は、そのループの開始位置。定数ではない。**
  `a > 0` の `0` に意味はない（`a` の開始位置がたまたま 0）。`two-pointers-template.html` に追加。
- **持ち込みの5例目、コードでは2例目。**今回は前問ではなく**同じ関数の4行上から**形を流用。
  → **「持ち込み」は距離と無関係に「形の流用」として起きる。**
- **`⚠` の7回連続が途切れた（摩耗）。**挙げたのは**たった今直したばかりの行**＝予測ではなく回想。
  → 以後 `⚠` は「**このセッションでまだ議論していない行**」と条件を付けて聞く。
  真の候補（未回収）：`while c < d and nums[c] == nums[c-1]`
  ＝**なぜ左側の重複だけで足りるのか**（外側固定なら、1つの左の値に対し和が一致する右は高々1つ）。
- **`sort()` を空間には計上するが時間には計上しない（16・18 の2回連続）。**空間側だけ修復されて時間側が残存。

### 次にやること
- 残る弱点は**すべて説明側の「言い忘れ」**：②のボトルネック（2回連続欠落）／`sort()` の時間計上（2回連続）／`⚠` の摩耗。
  アルゴリズムではないが、**ミッション（面接で声に出す）では致命的**。
  → 新しい型に移る前に、**既習問題1問で説明だけ回収する**（コードは書かない）。
- そのあと次の型へ。候補：binary search（「捨てる」の log n がここで初めて正当化される。LR-0011 の伏線回収）
  または monotonic stack。**模擬面接は8月下旬予定、未着手が最大のリスク。**

## Lesson 13（2026-07-26）— 説明だけの回収セッション、コードなし
- 新パターンなし。**LR-0018 の残り3点だけ**に絞った。題材は 15. 3Sum（既に通っている）。
- `sort()` の時間計上 → **「目で見えるものに変換」の4回目**：
  **⑥は関数の1行目から上から順に読む。ループを探しに行かない。**
  コスト台帳（行 × 1回あたり × 回数 × 小計）を表で提示し、`nums.sort()` の行を赤くした。
  - 論点は「吸収されるか」ではなく「**言うか**」。**黙って消えた行は数え忘れと区別がつかない。**
  - 対比として 217（ソート解）を出した。**同じ `sort()` が支配側になる**ので、
    「先に本体だけ見て決められない」＝1行目から読む理由になる。
    → NOTES 既出の「**例を出したら固有な部分と一般な部分を切り分ける**」の予防的適用。
- ②のボトルネック → **形式チェック：素朴解に出てくる語がそのまま入っているか。**
  入っていなければ最適解の説明。さらに「**素朴解と最適解を並べて引き算**」（3重 → 1重）で、
  探すのではなく出てくる形にした。
- `⚠` に条件を追加：**このセッションでまだ議論していない行から選ぶ。**
- `interview-protocol.html` の rubric に **[時間0] 前処理**の欄を新設（時間は4欄に）。
  ボトルネック欄に形式チェック、`⚠` に条件を追記。
- 課題：15 を**説明だけ**（⓪②③⑥⚠、コードは書かない）＋ 18 からの持ち越し
  （なぜ左側の重複だけスキップすれば足りるのか）。
- 検証点：(a) `sort()` が時間側で出るか（3回目の正直）(b) ボトルネック文に素朴解の語が入るか
  (c) `⚠` が未議論の行になるか。**3つとも通れば binary search へ移る。**

### 結果（LR-0019）— (a)(b) 通過、two pointers は説明側も完了
- **(a) 通過**：「O(n log n) は吸収されるため O(n²)」。**台帳＝「目で見えるものに変換」の4勝目。**
- **(b) 通過**：「3重目が全てを舐めてしまう」。形式チェック（素朴解の語が入っているか）が一発で効いた。
- 空間も2往復目で回収。**③は成分の取り違え**（`j` を動かしたのに列が消えると書いた）→ 1往復で修正。
  格子は生きている。落ちたのは**問題ごとの変数名の対応表**（18 は `c,d`／15 は `j,k`）。
  → **絵を渡すときは、その問題の変数名で対応を毎回言い直す。**
- 「`k` は配列内で最大」＝**残っている範囲の中で最大**。初期状態 `k=n-1` の付随的特徴が貼り付いた。
  「例の付随的特徴が概念に貼り付く」の4例目、**今回は例ではなく初期値**。

### 新しい弱点：storage strength（初観測・重要）
- 追い質問2つが**両方とも空**（「忘れちゃった」「わからない」）。うち1つは**資料に答えが書いてある**。
  15 は 07-23、18 は 07-25、今回 07-26。**3日で消えている。**
- これまでの失敗は全て「その場で言えない」型で、手続きを渡せば潰せた。
  **これは時間をまたぐ設計でしか潰せない。**
- → **以後、レッスン冒頭に retrieval 1問を置く。直前ではなく2回以上前の問題から引く（spacing）。**
- **正直な「忘れた」は歓迎すべき信号。**取り繕われると decay が見えない。減点しない。
- `⚠` の「このセッションで議論していない行」条件は、**日が空くと機能しない**（何を議論したかを忘れる）。
  代案「まだ一度も壊れたことのない行」は要検討。

### 次：binary search
- LR-0011 の伏線回収。**「捨てる → log n」がここで初めて正当化される。**
  Lesson 07〜08 で「two pointers は log n ではない（ギャップ論法）」と強く否定した相手なので、
  **否定形は肯定形とペアで**（LR-0015 の教訓）——「では、いつ log n になるのか」を今度こそ言う。
- 冒頭 retrieval の候補：11 の③（値ではなく上限）または 424 の⑥（持ち越し＝足し算）。**15/18 からは引かない。**

## Lesson 14（2026-07-26）— 新しい型：binary search 導入
- **冒頭 retrieval を初導入**（LR-0019 の処方）。題材は **問題11 の③（値ではなく上限、LR-0012）**。
  15/18 からは引かない（直前を避ける＝spacing）。**この1問が decay の計測器**でもある。
- 中心命題：**捨てる「理由」と捨てる「率」を分ける。**理由は two pointers と共通（ソート順による証明）。
  **log を生むのは率のほう**（1個 vs 半分）。→ **「ソート済みだから log n」は誤り**を明示的に潰した。
  - LR-0011（discard rate vs reason）の伏線回収。Lesson 07〜08 の否定形に対する**肯定形**（LR-0015 の教訓）。
  - クイズの誤答肢に「ソート済みだから」を置き、**LR-0016 の検査（別の解法にも当てはまらないか）**を再利用。
- ③ は two pointers と同じ手順で書く：**先に消える区間を名指し、次に理由**（LR-0017）。主語は**消えた区間**。
- **三つの決め事は連動する**（区間の取り方 / while の不等号 / 更新に ±1 が付くか）。一つ変えたら三つ見直す。
  終了性は **`right == left + 1` で1周させて範囲が真に狭まるか**——off-by-one（LR-0004）のこの型での現れ方。
- `bisect` を主資料に採用（techinterviewhandbook の binary-search ページは 403 で検証不能）。
  ドキュメントが「**ソート済みは前提として要求するが確認しない**」と明記 → ⓪（前提は誰の責任か）と直結。
- `reference/binary-search-template.html` を新設（前提／不変条件／型A厳密一致・型B境界／⑥の言い方／頻出バグ表）。
- 課題：704 を新形式（⓪②③ → 実装 → ⑥⚠）＋ 口頭1問
  **「どちらもソート済み配列を使うのに、なぜ two pointers は O(n) で二分探索は O(log n) か」**。
- 検証点：(a) 冒頭 retrieval が空でないか（storage の初計測）
  (b) ⑥ の根拠に**率**が出るか（「ソート済みだから」で止まらないか）
  (c) ③ が区間の名指しから始まるか　(d) `⚠` が未破壊の行になるか。

## 704 完答（2026-07-27、LR-0020）— binary search 1問目
- **⓪②③⑥すべて正答、LeetCode も通った。しかしコードは `l += 1` / `r -= 1` で実測 O(n)。**
  ⑥には「半分ずつ」と書いてある＝**主張と実装が正反対なのにテストが通る**。
  `l += 1` は正しさを壊さない（③は「mid+1 まで進めて**よい**」としか言っていない）ので、**遅さだけが残る**。
  → LR-0015（16 の `sort()`）と同型：**落ちないバグは最後まで疑われない。**
- 処方：**⑥で「毎回 X 捨てる」と言ったら、X を実現している行を指す。指せなければその行はまだ存在しない。**
  ⓪で確立した「保証している行の名指し」を⑥に移植。再提出で `mid` が文に入り解決。
  **「目で見えるものに変換」5勝目**（インデント→⑥／格子→③／台帳→前処理／更新行→率）。
- 通過：**⓪は満点で2回連続＝獲得済み**／②の形式チェック（素朴解の語）2回連続で機能／
  ③は2往復（「小さいとわかる」→「捨てても構わない」→ 正答）。**結論の一文が落ちる型の再発**だが、
  「何を探しているか。その区間にあるか」の2問で出た。**この2問を③の詰まり時の定型介入にする。**
- **`⚠` が急所を外した（2回連続）**。挙げたのは `while l <= r`（妥当だが今回の急所ではない）。
  → 選定条件を「**今日いちばん自信のない判断をした行**」に変更。「壊れたことのない行」は日が空くと判定不能。
- **冒頭 retrieval（11 の③）は空**＝2セッション連続。再供給済み。**次回冒頭で記述式で再テスト**（successive relearning）。
- ⑥の精緻化を1点指示：「半分にできる」→「**必ず半分になる**」。更新行は毎回実行されるので可能性ではない。

### 次：Lesson 15（型B＝境界を探す）
- 704 は型A（厳密一致）。**答えが「一致する要素」ではなく「境界の位置」になる型**へ。
  35. Search Insert Position → 278. First Bad Version。`bisect_left` に帰着することを見せる。
- 型Bでは `return mid` が使えない＝**ループを最後まで回して `left` を返す**。
  「三つの決め事」（区間 / 不等号 / ±1）が型Aと変わる点が本番。
- 冒頭に §0 として**照合ルール**（⑥の量 → 行の名指し）を常設装置化。

## Lesson 15（2026-07-27）— binary search 型B（境界を探す）
- 冒頭 retrieval を**記述式で2問**（選択式は通っても実装は通らない＝LR-0002）。
  (1) **11 の③「上限」の再テスト**（07-27 に「忘れた」と申告 → 再供給 → 再テスト＝successive relearning）
  (2) 704 の「半分を実現している行」＝照合ルールの retrieval。
- 中心命題：**答えは `return mid` ではなく、ループが止まった場所（`left`）にある。**
  型Aは「配列の中に答えがある」問題、型Bは「無くても答えがある」問題。
- **±1 が付くかは、`mid` がまだ候補かどうかで決まる**——「目で見えるものに変換」をこの型でも適用。
  `right = mid - 1` で答えを飛び越す具体例（`[1,3,5]`, target=3 → 添字1が消える）を記述式クイズに。
- 終了性は Lesson 14 と同じ手順（隣接2要素 `l=3, r=4` で1周させる）。
  `right = mid` が安全なのは `while left < right` と組んだときだけ、を明示（決め事の連動の実演）。
- 主資料：**CPython の `Lib/bisect.py`**（bisect_left/right が各7行、違いは不等号1文字）。
- 課題：35. Search Insert Position（新形式）→ 278. First Bad Version。
  278 は**配列が無い**＝「何がソートされていれば二分探索できるのか」を⓪で書かせる。
- 検証点：(a) **retrieval 2問が空でないか**（storage の2回目の計測、うち1問は再学習の効果測定）
  (b) ③に「`mid` は残す」が入るか（704 との違いの明示）
  (c) ⑥で**行を名指し**できるか（照合ルールの定着）
  (d) `⚠` が「迷った行」になるか（条件変更後の初回）。
